You are running transformation analysis only.
Repository: repo/spring-petclinic/
User query: $ARGUMENTS

STEP 1 — Apply query-agent skill
STEP 2 — Apply db-agent skill
STEP 3 — Apply app-agent skill
STEP 4 — Apply transform-agent skill

Show ONLY the transformation chain.
Skip graph, JSON and report output.
Save to lineage-results/<attribute>_transforms.txt
