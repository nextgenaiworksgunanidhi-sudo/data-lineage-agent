List all traceable attributes in the repository by
reading ast-output/java-ast.json and ast-output/sql-ast.json.

Show a clean table:
  ENTITY | JAVA FIELD | DB TABLE | DB COLUMN | TYPE

Then suggest 5 example /lineage queries the user
can run based on what actually exists in the codebase.
